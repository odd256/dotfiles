# Dotfiles for Arch Linux

Configuration files used to customize and manage your Linux desktop environment.

## How to use

0. Before copy, make sure you install all the required packages. 
1. Copy `.config` files to `~/config`.
2. Copy `scripts` files to `/usr/local/bin/`.
3. Reboot to apply, and enjoy :)
